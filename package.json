{
  "name": "@kujenga/site",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "./scripts/postinstall.sh",
    "start": "hugo server --watch --verbose",
    "build": "gulp pre && hugo && gulp post",
    "deploy": "npm run build && ./scripts/deploy.sh"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-htmlhint": "^2.2.1",
    "gulp-imagemin": "^6.1.0",
    "gulp-rev": "^9.0.0",
    "gulp-rev-delete-original": "^0.2.3",
    "gulp-rev-rewrite": "^2.0.0"
  }
}
