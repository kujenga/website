{
  "name": "@kujenga/site",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "./scripts/postinstall.sh",
    "start": "hugo server --watch --verbose",
    "build": "gulp && hugo",
    "deploy": "./scripts/deploy.sh"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-imagemin": "^6.1.0"
  }
}
